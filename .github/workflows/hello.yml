# name: My First Action

# on: push

# jobs:
#   hello:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Print Message
#         run: echo "Hello Ajay!"

# name: Learn GitHub Actions

# on:
#   push:

# jobs:
#   hello:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Print Date
#         run: date

#       - name: Show Current Directory
#         run: pwd

#       - name: Show Node Version
#         run: node -v

# name: Checkout Demo

# on:
#   push:

# jobs:
#   demo:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - run: ls -la

# name: Build Project

# on:
#   push:

# jobs:
#   backend-build:
#     runs-on: ubuntu-latest

#     defaults:
#       run:
#         working-directory: backend

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 22

#       - run: npm install
#       - run: npm run build

#   frontend-build:
#     runs-on: ubuntu-latest

#     defaults:
#       run:
#         working-directory: frontend

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 22

#       - run: npm install
#       - run: npm run build

# name: Build Project

# on:
#   push:

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     strategy:
#       matrix:
#         app: [backend, frontend]

#     defaults:
#       run:
#         working-directory: ${{ matrix.app }}

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 22

#       - run: npm install
#       - run: npm run build

#       - name: Build Docker Image
#         run: docker build -t ${{ matrix.app }} .

name: Build Project

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        app: [backend, frontend]

    defaults:
      run:
        working-directory: ${{ matrix.app }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: npm install
      - run: npm run build

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: |
          docker build \
            -t ${{ secrets.DOCKER_USERNAME }}/product-management-${{ matrix.app }}:latest \
            .

      - name: Push Docker Image
        run: |
          docker push \
            ${{ secrets.DOCKER_USERNAME }}/product-management-${{ matrix.app }}:latest
